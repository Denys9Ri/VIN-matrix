from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    # Підключаємо наші API маршрути:
    path('api/integrations/', include('apps.integrations.urls')),
]
