const fs = require('fs');
const path = require('path');

const file = path.resolve(__dirname, '../src/pages/StoreOrdersDictionaryBoard.jsx');
let src = fs.readFileSync(file, 'utf8');

// Surgical fix for PartsPanel JSX: parts.map(...) had one extra closing parenthesis.
src = src.replace(
  'onClick={() => onDelete(p)} className="px-2 py-1 rounded-lg bg-rose-50 text-rose-600"><Trash2 size={14}/></button></div></div></div>))}</div></Panel>; }',
  'onClick={() => onDelete(p)} className="px-2 py-1 rounded-lg bg-rose-50 text-rose-600"><Trash2 size={14}/></button></div></div></div>)}</div></Panel>; }'
);

if (!src.includes('const fallbackOrderSources = [')) {
  src = src.replace(
    "const emptyOrder = { client: '', phone: '', plate: '', vin_code: '', source: 'Телефон', delivery_type: 'pickup', payment_status: 'unpaid', comment: '' };",
    "const emptyOrder = { client: '', phone: '', plate: '', vin_code: '', source: 'phone', delivery_type: 'pickup', payment_status: 'unpaid', comment: '' };\nconst fallbackOrderSources = [\n  { key: 'phone', label: 'Телефон' },\n  { key: 'market', label: 'Авторинок' },\n  { key: 'prom', label: 'Prom.ua' },\n  { key: 'rozetka', label: 'Rozetka' },\n  { key: 'instagram', label: 'Instagram' },\n  { key: 'telegram', label: 'Telegram' },\n  { key: 'regular_client', label: 'Постійний клієнт' },\n  { key: 'recommendation', label: 'Рекомендація' },\n];\nconst fallbackCancelReasons = [\n  { key: 'changed_mind', label: 'Клієнт передумав' },\n  { key: 'expensive', label: 'Дорого' },\n  { key: 'no_answer', label: 'Не дозвонились' },\n  { key: 'found_cheaper', label: 'Знайшов дешевше' },\n  { key: 'not_available', label: 'Немає товару' },\n  { key: 'too_long', label: 'Довго чекати' },\n  { key: 'selection_error', label: 'Помилка підбору' },\n  { key: 'other', label: 'Інше' },\n];"
  );
}

if (!src.includes('function optionLabel(options')) {
  src = src.replace(
    "function hint(order, statusLabel) { const d = parseDelivery(order), parts = arr(order.parts); if (d.delivery_status === 'sent') return `Статус: ${statusLabel}. Замовлення відправлено.`; if (!parts.length) return `Статус: ${statusLabel}. Додайте товари, щоб вести замовлення далі.`; if (parts.some((p) => ['WAITING', 'IN_TRANSIT', 'ORDERED'].includes(p.status || 'WAITING'))) return `Статус: ${statusLabel}. Є товари в очікуванні.`; return `Статус: ${statusLabel}. Усі товари отримано.`; }",
    "function hint(order, statusLabel) { const d = parseDelivery(order), parts = arr(order.parts); if (d.delivery_status === 'sent') return `Статус: ${statusLabel}. Замовлення відправлено.`; if (!parts.length) return `Статус: ${statusLabel}. Додайте товари, щоб вести замовлення далі.`; if (parts.some((p) => ['WAITING', 'IN_TRANSIT', 'ORDERED'].includes(p.status || 'WAITING'))) return `Статус: ${statusLabel}. Є товари в очікуванні.`; return `Статус: ${statusLabel}. Усі товари отримано.`; }\nfunction optionLabel(options, key) { const item = arr(options).find((x) => String(x.key) === String(key) || String(x.label) === String(key)); return item?.label || key || '—'; }\nfunction activeOptions(list, fallback) { const source = arr(list).filter((x) => x.is_active !== false); return source.length ? source : fallback; }"
  );
}

src = src.replace("const [statuses, setStatuses] = useState([]), [partStatuses, setPartStatuses] = useState([]);", "const [statuses, setStatuses] = useState([]), [partStatuses, setPartStatuses] = useState([]), [orderSources, setOrderSources] = useState(fallbackOrderSources), [cancelReasons, setCancelReasons] = useState(fallbackCancelReasons);");
src = src.replace("const loadDictionaries = async () => { try { const r = await api.get('/api/settings/dictionaries/?mode=store'); setStatuses(arr(r.data?.store_order_status)); setPartStatuses(arr(r.data?.part_status)); } catch { setStatuses([]); setPartStatuses([]); } };", "const loadDictionaries = async () => { try { const r = await api.get('/api/settings/dictionaries/?mode=store'); setStatuses(arr(r.data?.store_order_status)); setPartStatuses(arr(r.data?.part_status)); setOrderSources(activeOptions(r.data?.order_source, fallbackOrderSources)); setCancelReasons(activeOptions(r.data?.cancel_reason, fallbackCancelReasons)); } catch { setStatuses([]); setPartStatuses([]); setOrderSources(fallbackOrderSources); setCancelReasons(fallbackCancelReasons); } };");
src = src.replace("const createOrder = async (e) => { e.preventDefault(); setBusy(true); try { const delivery = { source: orderForm.source, delivery_status: 'draft', mode: 'store' };", "const createOrder = async (e) => { e.preventDefault(); setBusy(true); try { const delivery = { source: orderForm.source, source_label: optionLabel(orderSources, orderForm.source), delivery_status: 'draft', mode: 'store' };");
src = src.replace("const cancelOrder = async (reason) => { const delivery = { ...parseDelivery(selected), cancel_reason: reason }; await patchOrder({ status: 'CANCELLED', delivery_data: JSON.stringify(delivery) }); setModal(null); };", "const cancelOrder = async (reason) => { const key = reason?.key || reason; const delivery = { ...parseDelivery(selected), cancel_reason: key, cancel_reason_label: reason?.label || optionLabel(cancelReasons, key) }; await patchOrder({ status: 'CANCELLED', delivery_data: JSON.stringify(delivery) }); setModal(null); };");
src = src.replace("delivery_data: JSON.stringify({ source: 'Швидкий продаж', mode: 'store', quick_sale: true, delivery_status: 'received' }),", "delivery_data: JSON.stringify({ source: 'quick_sale', source_label: 'Швидкий продаж', mode: 'store', quick_sale: true, delivery_status: 'received' }),");
src = src.replace("<Drawer order={selected} tab={tab} setTab={setTab} setOrder={setSelected} onPatch={patchOrder} onPay={markPaid}", "<Drawer order={selected} tab={tab} setTab={setTab} setOrder={setSelected} orderSources={orderSources} onPatch={patchOrder} onPay={markPaid}");
src = src.replace("<OrderForm form={orderForm} setForm={setOrderForm} onSubmit={createOrder} busy={busy} />", "<OrderForm form={orderForm} setForm={setOrderForm} onSubmit={createOrder} busy={busy} orderSources={orderSources} />");
src = src.replace("<CancelModal onClose={() => setModal(null)} onSubmit={cancelOrder} busy={busy} />", "<CancelModal onClose={() => setModal(null)} onSubmit={cancelOrder} busy={busy} cancelReasons={cancelReasons} />");
src = src.replace("function Drawer(p) { const { order, tab, setTab, setOrder, onPatch, onPay, onPrepay, onUpdateDelivery, onCancel, partForm, setPartForm, onAddPart, onPartStatus, onDelete, statuses, partStatuses, getStatusLabel } = p;", "function Drawer(p) { const { order, tab, setTab, setOrder, orderSources = fallbackOrderSources, onPatch, onPay, onPrepay, onUpdateDelivery, onCancel, partForm, setPartForm, onAddPart, onPartStatus, onDelete, statuses, partStatuses, getStatusLabel } = p;");
src = src.replace('<Info label="Джерело" value={d.source || \'-\'} />', '<Info label="Джерело" value={d.source_label || optionLabel(orderSources, d.source)} />');

const oldOrderForm = `function OrderForm({ form, setForm, onSubmit, busy }) { return <form onSubmit={onSubmit} className="grid grid-cols-1 gap-3"><input required placeholder="Клієнт" value={form.client} onChange={(e) => setForm({ ...form, client: e.target.value })} className={field} /><input placeholder="Телефон" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} className={field} /><input placeholder="Авто / номер" value={form.plate} onChange={(e) => setForm({ ...form, plate: e.target.value })} className={field} /><input placeholder="VIN" value={form.vin_code} onChange={(e) => setForm({ ...form, vin_code: e.target.value })} className={field} /><textarea placeholder="Коментар" value={form.comment} onChange={(e) => setForm({ ...form, comment: e.target.value })} className={field} /><button disabled={busy} className={primaryBtn}>Створити</button></form>; }`;
const newOrderForm = `function OrderForm({ form, setForm, onSubmit, busy, orderSources = fallbackOrderSources }) { return <form onSubmit={onSubmit} className="grid grid-cols-1 gap-3"><input required placeholder="Клієнт" value={form.client} onChange={(e) => setForm({ ...form, client: e.target.value })} className={field} /><input placeholder="Телефон" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} className={field} /><input placeholder="Авто / номер" value={form.plate} onChange={(e) => setForm({ ...form, plate: e.target.value })} className={field} /><input placeholder="VIN" value={form.vin_code} onChange={(e) => setForm({ ...form, vin_code: e.target.value })} className={field} /><select value={form.source || 'phone'} onChange={(e) => setForm({ ...form, source: e.target.value })} className={field}>{orderSources.map((source) => <option key={source.key} value={source.key}>{source.label}</option>)}</select><textarea placeholder="Коментар" value={form.comment} onChange={(e) => setForm({ ...form, comment: e.target.value })} className={field} /><button disabled={busy} className={primaryBtn}>Створити</button></form>; }`;
if (src.includes(oldOrderForm)) src = src.replace(oldOrderForm, newOrderForm);
const oldCancelModal = `function CancelModal({ onClose, onSubmit, busy }) { const [reason, setReason] = useState('Клієнт передумав'); return <Modal title="Скасувати замовлення" onClose={onClose}><select value={reason} onChange={(e) => setReason(e.target.value)} className={field}><option>Клієнт передумав</option><option>Дорого</option><option>Немає в наявності</option><option>Довго чекати</option><option>Інше</option></select><button disabled={busy} onClick={() => onSubmit(reason)} className="mt-3 w-full bg-rose-600 text-white rounded-2xl py-3 font-black uppercase text-xs">Скасувати</button></Modal>; }`;
const newCancelModal = `function CancelModal({ onClose, onSubmit, busy, cancelReasons = fallbackCancelReasons }) { const [reason, setReason] = useState(cancelReasons[0]?.key || 'changed_mind'); const selected = cancelReasons.find((item) => item.key === reason) || cancelReasons[0]; return <Modal title="Скасувати замовлення" onClose={onClose}><select value={reason} onChange={(e) => setReason(e.target.value)} className={field}>{cancelReasons.map((item) => <option key={item.key} value={item.key}>{item.label}</option>)}</select><button disabled={busy} onClick={() => onSubmit(selected || reason)} className="mt-3 w-full bg-rose-600 text-white rounded-2xl py-3 font-black uppercase text-xs">Скасувати</button></Modal>; }`;
if (src.includes(oldCancelModal)) src = src.replace(oldCancelModal, newCancelModal);

if (!src.includes('const npStatusLabel')) {
  src = src.replace("const pay = { unpaid: 'Не оплачено', prepaid: 'Передплата', paid: 'Оплачено', cod: 'Післяплата', debt: 'Борг' };", "const pay = { unpaid: 'Не оплачено', prepaid: 'Передплата', paid: 'Оплачено', cod: 'Післяплата', debt: 'Борг' };\nconst npStatusLabel = { draft: 'Чернетка', manual: 'ТТН внесено', tracking: 'Перевіряється', in_transit: 'У дорозі', arrived: 'Прибуло у відділення', received: 'Отримано', returned: 'Повернення' };");
}

src = src.replace(
  "function DeliveryPanel({ delivery, onPatch, onUpdateDelivery }) { const [ttn, setTtn] = useState(delivery.ttn || ''); useEffect(() => setTtn(delivery.ttn || ''), [delivery.ttn]); return <Panel title=\"Нова пошта / доставка\"><div className=\"grid grid-cols-1 md:grid-cols-[1fr_auto_auto] gap-2\"><input value={ttn} onChange={(e) => setTtn(e.target.value)} placeholder=\"ТТН\" className={field} /><button onClick={() => onUpdateDelivery({ ttn })} className={primaryBtn}>Зберегти ТТН</button><button onClick={() => onUpdateDelivery({ delivery_status: 'sent', ttn })} className={greenBtn}><Send size={16}/> Відправлено</button></div><button onClick={() => onPatch({ status: 'COMPLETED' })} className=\"mt-3 bg-slate-900 text-white rounded-2xl px-5 py-3 text-xs font-black uppercase\">Закрити як виконане</button></Panel>; }",
  "function DeliveryPanel({ order, delivery, onPatch, onUpdateDelivery }) { const [profiles, setProfiles] = useState([]), [np, setNp] = useState(null), [ttn, setTtn] = useState(delivery.ttn || ''), [profileId, setProfileId] = useState(''), [loading, setLoading] = useState(false), [msg, setMsg] = useState(''); useEffect(() => { let off = false; api.get(`/api/delivery/novapost/visits/${order.id}/`).then((r) => { if (off) return; setProfiles(arr(r.data?.profiles)); const d = r.data?.delivery || null; setNp(d); setTtn(d?.ttn || delivery.ttn || ''); setProfileId(d?.novapost_profile_id || ''); }).catch(() => setMsg('Додайте профіль Нової пошти в налаштуваннях доставки.')); return () => { off = true; }; }, [order.id]); const save = async () => { setLoading(true); setMsg(''); try { const r = await api.post(`/api/delivery/novapost/visits/${order.id}/`, { ttn, novapost_profile_id: profileId || null, status: 'manual', recipient_name: order.client, recipient_phone: order.phone }); setNp(r.data?.delivery); await onUpdateDelivery({ ttn, delivery_status: 'sent', np_status: r.data?.delivery?.status, np_status_text: r.data?.delivery?.status_text }); setMsg('ТТН збережено.'); } catch (e) { setMsg(e.response?.data?.error || 'Не вдалося зберегти ТТН.'); } finally { setLoading(false); } }; const check = async () => { setLoading(true); setMsg(''); try { const r = await api.post(`/api/delivery/novapost/visits/${order.id}/status/`); const d = r.data?.delivery; setNp(d); await onUpdateDelivery({ ttn: d?.ttn, delivery_status: d?.status === 'received' ? 'received' : 'sent', np_status: d?.status, np_status_text: d?.status_text }); setMsg(r.data?.message || 'Статус оновлено.'); } catch (e) { setMsg(e.response?.data?.error || 'Не вдалося оновити статус.'); } finally { setLoading(false); } }; const events = arr(np?.tracking_data?.events); return <Panel title=\"Нова пошта / доставка\"><div className=\"rounded-3xl bg-gradient-to-r from-slate-900 via-blue-900 to-cyan-700 text-white p-4 mb-4\"><p className=\"text-[10px] font-black uppercase text-blue-100\">ТТН: {np?.ttn || ttn || 'ще не внесено'}</p><h3 className=\"text-2xl font-black mt-1\">{npStatusLabel[np?.status] || np?.status_text || 'Ручне внесення ТТН'}</h3><p className=\"text-sm font-bold text-blue-50/80 mt-1\">{np?.status_text || 'Внесіть ТТН, виберіть профіль і перевірте статус.'}</p></div>{msg && <div className=\"mb-3 bg-blue-50 border border-blue-100 text-blue-800 rounded-2xl px-4 py-3 text-sm font-bold\">{msg}</div>}<div className=\"grid grid-cols-1 md:grid-cols-2 gap-2\"><select value={profileId} onChange={(e) => setProfileId(e.target.value)} className={field}><option value=\"\">Профіль НП за замовчуванням</option>{profiles.map((p) => <option key={p.id} value={p.id}>{p.is_default ? '★ ' : ''}{p.name}</option>)}</select><input value={ttn} onChange={(e) => setTtn(e.target.value)} placeholder=\"ТТН Нової пошти\" className={field} /></div><div className=\"flex flex-wrap gap-2 mt-3\"><button disabled={loading} onClick={save} className={primaryBtn}>{loading ? 'Зачекайте...' : 'Зберегти ТТН'}</button><button disabled={loading || !(np?.ttn || ttn)} onClick={check} className={greenBtn}><Truck size={16}/> Оновити статус</button><button disabled={!(np?.ttn || ttn)} onClick={() => window.open(['https://nova','poshta.ua/tracking/?cargo_number='].join('') + encodeURIComponent(np?.ttn || ttn), '_blank')} className=\"inline-flex items-center justify-center gap-2 rounded-2xl bg-slate-900 px-5 py-3 text-xs font-black uppercase text-white hover:bg-black disabled:opacity-50\">Відкрити НП</button><button onClick={() => onPatch({ status: 'COMPLETED' })} className=\"inline-flex items-center justify-center gap-2 rounded-2xl bg-slate-100 px-5 py-3 text-xs font-black uppercase text-slate-700 hover:bg-slate-200\">Закрити як виконане</button></div><div className=\"mt-5\"><h4 className=\"font-black uppercase text-xs text-slate-400 mb-3\">Історія трекінгу</h4>{events.length ? <div className=\"space-y-2\">{events.map((e, i) => <div key={i} className=\"bg-slate-50 border rounded-2xl p-3\"><p className=\"text-[10px] font-black uppercase text-slate-400\">{e.time ? new Date(e.time).toLocaleString('uk-UA') : '—'} · {npStatusLabel[e.status] || e.status}</p><p className=\"font-bold text-slate-800 mt-1\">{e.text || 'Оновлено статус'}</p></div>)}</div> : <div className=\"bg-slate-50 border rounded-2xl p-5 text-center text-slate-400 font-black uppercase text-xs\">Історія зʼявиться після першої перевірки статусу</div>}</div></Panel>; }"
);

fs.writeFileSync(file, src);
console.log('Store orders patch applied, including manual Nova Post TTN tracking UI.');

// Settings/client-link patch removed from this script intentionally: settings are patched directly in source files.
