import React,{useEffect}from'react';import{X}from'lucide-react';import Button from './Button';
export default function Drawer({open,onClose,title,children,side='right'}){useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.body.style.overflow=previous}},[open]);if(!open)return null;return <div className="fixed inset-0 z-50 bg-slate-950/40" onMouseDown={e=>{if(e.target===e.currentTarget)onClose?.()}}><aside role="dialog" aria-modal="true" aria-label={title||'Панель'} className={`absolute ${side==='left'?'left-0':'right-0'} top-0 h-[100dvh] w-full max-w-md overflow-y-auto bg-white p-5 pb-[max(1.25rem,env(safe-area-inset-bottom))] shadow-2xl`}><div className="mb-5 flex items-center justify-between gap-3"><h2 className="min-w-0 break-words text-xl font-black text-slate-900">{title}</h2><Button variant="ghost" size="sm" iconOnly icon={<X/>} onClick={onClose} aria-label="Закрити"/></div>{children}</aside></div>}