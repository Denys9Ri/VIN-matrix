import React,{useState}from'react';import{Eye,EyeOff}from'lucide-react';import{cn}from'./utils';
export default function Input({label,error,helperText,icon,type='text',password=false,uppercase=false,className='',inputClassName='',onChange,...props}){const[show,setShow]=useState(false);const inputType=password?(show?'text':'password'):type;return <label className={cn('block space-y-1.5',className)}>{label&&<span className="text-sm font-semibold text-slate-700">{label}{props.required&&<span className="text-rose-500"> *</span>}</span>}<span className="relative block">{icon&&<span className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400">{icon}</span>}<input type={inputType} className={cn('w-full rounded-2xl border bg-white px-4 py-3 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-blue-500 focus:ring-4 focus:ring-blue-100 disabled:bg-slate-100',icon&&'pl-10',password&&'pr-11',error?'border-rose-300':'border-slate-200',inputClassName)} onChange={e=>{if(uppercase)e.target.value=e.target.value.toUpperCase();onChange?.(e)}} {...props}/>{password&&<button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button>}</span>{(error||helperText)&&<p className={cn('text-xs',error?'text-rose-600':'text-slate-500')}>{error||helperText}</p>}</label>}
