import React from 'react';import EmptyState from './EmptyState';import Skeleton from './Skeleton';import{cn}from'./utils';
export default function Table({columns=[],rows=[],loading=false,emptyTitle,emptyDescription,renderMobileCard,stickyFirstColumn=false}){if(loading)return <div className="rounded-3xl border border-slate-200 bg-white p-4"><Skeleton rows={5}/></div>;if(!rows?.length)return <EmptyState title={emptyTitle} description={emptyDescription}/>;return <div><div className="hidden overflow-x-auto rounded-3xl border border-slate-200 bg-white shadow-sm md:block"><table className="min-w-full divide-y divide-slate-100"><thead className="bg-slate-50"><tr>{columns.map((c,i)=><th key={c.key||i} className={cn('px-4 py-3 text-left text-xs font-black uppercase tracking-wide text-slate-500',stickyFirstColumn&&i===0&&'sticky left-0 bg-slate-50')}>{c.header}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((r,ri)=><tr key={r.id||ri} className="hover:bg-slate-50/70">{columns.map((c,ci)=><td key={c.key||ci} className={cn('px-4 py-3 text-sm text-slate-700',stickyFirstColumn&&ci===0&&'sticky left-0 bg-white font-bold')}>{c.render?c.render(r):r[c.key]}</td>)}</tr>)}</tbody></table></div><div className="space-y-3 md:hidden">{rows.map((r,i)=>renderMobileCard?renderMobileCard(r,i):<div key={r.id||i} className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm">{columns.map(c=><div key={c.key} className="flex justify-between gap-3 py-1 text-sm"><span className="text-slate-500">{c.header}</span><span className="font-semibold text-slate-800">{c.render?c.render(r):r[c.key]}</span></div>)}</div>)}</div></div>}
