import React from 'react';import{cn}from'./utils';
export default function Tabs({items=[],value,onChange,className=''}){return <div className={cn('mb-5 overflow-x-auto',className)}><div className="inline-flex min-w-full gap-2 rounded-2xl bg-slate-100 p-1 sm:min-w-0">{items.map(t=><button key={t.value} onClick={()=>onChange?.(t.value)} className={cn('flex items-center gap-2 whitespace-nowrap rounded-xl px-4 py-2 text-sm font-bold transition',value===t.value?'bg-white text-blue-700 shadow-sm':'text-slate-600 hover:text-slate-900')}>{t.icon}{t.label}{t.badge!=null&&<span className="rounded-full bg-slate-200 px-2 py-0.5 text-xs">{t.badge}</span>}</button>)}</div></div>}
