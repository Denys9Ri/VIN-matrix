import React, { useEffect, useMemo, useState } from 'react';
import { CalendarDays, CheckCircle2, ChevronLeft, ChevronRight, Clock, Copy, PackageCheck, Plus, Printer, Search, Send, ShoppingCart, Trash2, Truck, X, XCircle } from 'lucide-react';
import api from '../api/axios';

const emptyOrder = { client: '', phone: '', plate: '', vin_code: '', source: 'Телефон', delivery_type: 'pickup', city: '', warehouse: '', recipient: '', recipient_phone: '', ttn: '', payment_status: 'unpaid', comment: '' };
const emptyPart = { name: '', brand: '', article: '', supplier: '', buy_price: '', sell_price: '', quantity: 1, status: 'WAITING' };
const emptyQuickSale = { name: '', brand: '', article: '', supplier: 'Склад / магазин', buy_price: '', sell_price: '', quantity: 1, payment_status: 'paid', comment: '' };
const arr = (value) => Array.isArray(value) ? value : [];
const money = (value) => `${Number(value || 0).toLocaleString('uk-UA', { maximumFractionDigits: 2 })} ₴`;
const qty = (value) => Number(value || 1).toLocaleString('uk-UA', { maximumFractionDigits: 2 }).replace(',00', '');
const num = (value) => Number(String(value || 0).replace(',', '.')) || 0;
const dateISO = (date = new Date()) => `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`;
const humanDate = (value) => { const d = new Date(`${value}T12:00:00`); return Number.isNaN(d.getTime()) ? 'Сьогодні' : d.toLocaleDateString('uk-UA', { weekday: 'short', day: '2-digit', month: 'long' }); };
const shiftDate = (value, days) => { const d = new Date(`${value}T12:00:00`); d.setDate(d.getDate() + days); return dateISO(d); };
const fieldClass = 'w-full bg-slate-50 border-2 border-slate-200 rounded-xl px-4 py-3 text-sm font-bold text-slate-800 outline-none focus:bg-white focus:border-blue-500 placeholder:text-slate-400 transition-all';
const paymentLabel = { unpaid: 'Не оплачено', prepaid: 'Передплата', paid: 'Оплачено', cod: 'Післяплата', debt: 'Борг' };
const deliveryStatusLabel = { draft: 'Не відправлено', sent: 'Відправлено', received: 'Отримано', returned: 'Повернення' };
const cancelReasons = ['Клієнт передумав', 'Дорого', 'Не підійшло', 'Немає в наявності', 'Довга доставка', 'Знайшов дешевше', 'Дубль замовлення', 'Інше'];

const parseDelivery = (order) => {
  if (!order?.delivery_data || typeof order.delivery_data !== 'string' || !order.delivery_data.trim().startsWith('{')) return {};
  try { return JSON.parse(order.delivery_data) || {}; } catch { return {}; }
};
const orderDateText = (order) => { const d = new Date(order.scheduled_datetime || order.created_at || order.updated_at || Date.now()); return Number.isNaN(d.getTime()) ? 'Без дати' : d.toLocaleString('uk-UA', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }); };
const partCalc = (p) => { const quantity = num(p.quantity || 1) || 1; const buy = num(p.buy_price); const sell = num(p.sell_price); const revenue = sell * quantity; const cost = buy * quantity; const profit = revenue - cost; const margin = revenue > 0 ? (profit / revenue) * 100 : 0; return { quantity, buy, sell, revenue, cost, profit, margin }; };
const orderTotals = (order) => { const totals = arr(order.parts).reduce((acc, p) => { const c = partCalc(p); acc.revenue += c.revenue; acc.cost += c.cost; acc.profit += c.profit; return acc; }, { revenue: 0, cost: 0, profit: 0 }); totals.margin = totals.revenue > 0 ? (totals.profit / totals.revenue) * 100 : 0; totals.count = arr(order.parts).length; return totals; };
const partSourceLabel = (p) => p?.source_label || p?.supplier || (p?.inventory_item ? 'Мій склад' : 'Постачальник не вказаний');
const stockLabel = (p) => p?.stock_status === 'sold' ? 'Склад: списано' : p?.stock_status === 'reserved' ? 'Склад: резерв' : p?.stock_status === 'released' ? 'Склад: резерв знято' : p?.inventory_item ? 'Склад: знайдено' : 'Склад: не знайдено';

const boardColumns = [
  { key: 'processing', title: 'В обробці', icon: Clock, tone: 'amber', match: (o) => ['SELECTION', 'PENDING', 'DRAFT'].includes(o.status) },
  { key: 'waiting', title: 'Очікує товар', icon: Truck, tone: 'blue', match: (o) => ['IN_PROGRESS', 'ORDERED'].includes(o.status) },
  { key: 'ready', title: 'Готове / Відправлено', icon: Send, tone: 'indigo', match: (o) => o.status === 'DONE' },
  { key: 'completed', title: 'Виконано', icon: CheckCircle2, tone: 'emerald', match: (o) => o.status === 'COMPLETED' },
];
const columnStyle = { amber: 'border-amber-200 bg-amber-50/70 text-amber-700', blue: 'border-blue-200 bg-blue-50/70 text-blue-700', indigo: 'border-indigo-200 bg-indigo-50/70 text-indigo-700', emerald: 'border-emerald-200 bg-emerald-50/70 text-emerald-700' };
const iconStyle = { amber: 'bg-amber-100 text-amber-700', blue: 'bg-blue-100 text-blue-700', indigo: 'bg-indigo-100 text-indigo-700', emerald: 'bg-emerald-100 text-emerald-700' };
const nextStatus = (order) => { const parts = arr(order.parts); if (!parts.length) return 'SELECTION'; const active = parts.filter((p) => (p.status || 'WAITING') !== 'UNAVAILABLE'); if (active.length && active.every((p) => (p.status || 'WAITING') === 'ARRIVED')) return 'DONE'; if (parts.some((p) => ['WAITING', 'IN_TRANSIT'].includes(p.status || 'WAITING'))) return 'ORDERED'; return order.status || 'SELECTION'; };
const getSuggestion = (order) => { const delivery = parseDelivery(order); const parts = arr(order.parts); if (!parts.length) return { text: 'Додайте товари — замовлення поки в обробці.', label: 'Залишити в обробці', status: 'SELECTION' }; if (parts.some((p) => ['WAITING', 'IN_TRANSIT'].includes(p.status || 'WAITING'))) return { text: 'Є товари, які ще треба замовити або отримати.', label: 'Перевести в очікує товар', status: 'ORDERED' }; if (parts.every((p) => (p.status || 'WAITING') === 'ARRIVED') && order.status !== 'DONE') return { text: 'Усі товари отримано — замовлення можна готувати до видачі.', label: 'Перевести в готове', status: 'DONE' }; if (delivery.ttn && delivery.delivery_status !== 'sent') return { text: 'ТТН додано — можна позначити замовлення відправленим.', label: 'Позначити відправленим', deliveryStatus: 'sent', status: 'DONE' }; if ((order.payment_status === 'paid' || order.payment_status === 'cod') && ['DONE'].includes(order.status)) return { text: 'Оплата/відправка готові — можна закрити замовлення.', label: 'Закрити як виконане', status: 'COMPLETED' }; return { text: 'Замовлення виглядає актуальним. Перевірте оплату й доставку.', label: 'Авто-статус', status: nextStatus(order) }; };

export default function StoreOrders() {
  const [orders, setOrders] = useState([]);
  const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState('');
  const [search, setSearch] = useState('');
  const [filterDate, setFilterDate] = useState(dateISO());
  const [modal, setModal] = useState(null);
  const [selected, setSelected] = useState(null);
  const [orderForm, setOrderForm] = useState({ ...emptyOrder });
  const [partForm, setPartForm] = useState({ ...emptyPart });
  const [quickSaleForm, setQuickSaleForm] = useState({ ...emptyQuickSale });
  const [busy, setBusy] = useState(false);

  const load = async () => { setLoading(true); try { const res = await api.get(`/api/visits/?date=${filterDate}`); setOrders(Array.isArray(res.data) ? res.data : []); } catch { setMessage('Не вдалося завантажити замовлення.'); } finally { setLoading(false); } };
  useEffect(() => { load(); }, [filterDate]);
  const refreshSelected = async (id = selected?.id) => { if (!id) return; try { const res = await api.get(`/api/visits/${id}/`); setSelected(res.data); setOrders((prev) => prev.map((o) => o.id === res.data.id ? res.data : o)); } catch { load(); } };
  const filtered = useMemo(() => { const q = search.toLowerCase().trim(); return orders.filter((order) => { const d = parseDelivery(order); const text = [`№${order.id}`, String(order.id), order.client, order.phone, order.plate, order.vin_code, order.comment, d.source, d.city, d.warehouse, d.ttn, ...arr(order.parts).flatMap((p) => [p.brand, p.article, p.name, p.supplier, p.source_label])].filter(Boolean).join(' ').toLowerCase(); return !q || text.includes(q.replace('№', '')) || text.includes(q); }); }, [orders, search]);
  const groups = useMemo(() => boardColumns.map((column) => ({ ...column, orders: filtered.filter(column.match) })), [filtered]);

  const createOrder = async (e) => { e.preventDefault(); setBusy(true); const delivery = { source: orderForm.source, city: orderForm.city, warehouse: orderForm.warehouse, recipient: orderForm.recipient, recipient_phone: orderForm.recipient_phone, ttn: orderForm.ttn, delivery_status: 'draft', delivery_cost: '', cod_amount: '', cod_commission: '', mode: 'store' }; const payload = { client: orderForm.client.trim(), phone: orderForm.phone.trim() || '0000000000', plate: (orderForm.plate || `ORDER-${Date.now()}`).trim().toUpperCase(), vin_code: orderForm.vin_code.trim().toUpperCase() || null, status: 'SELECTION', delivery_type: orderForm.delivery_type, delivery_data: JSON.stringify(delivery), payment_status: orderForm.payment_status, prepayment_amount: 0, comment: orderForm.comment, scheduled_datetime: new Date(`${filterDate}T12:00:00`).toISOString() }; try { const res = await api.post('/api/visits/', payload); setOrders((prev) => [res.data, ...prev]); setSelected(res.data); setModal(null); setMessage('Замовлення створено. Додайте товари.'); } catch (error) { setMessage(error.response?.data?.error || 'Не вдалося створити замовлення.'); } finally { setBusy(false); } };
  const addPart = async (e) => { e.preventDefault(); if (!selected?.id) return; setBusy(true); try { await api.post('/api/order-parts/', { visit: selected.id, name: partForm.name.trim(), brand: (partForm.brand || 'Без бренду').trim(), article: (partForm.article || 'manual').trim(), supplier: (partForm.supplier || 'Ручне додавання').trim(), buy_price: num(partForm.buy_price), sell_price: num(partForm.sell_price), quantity: num(partForm.quantity) || 1, status: partForm.status || 'WAITING' }); setPartForm({ ...emptyPart }); await refreshSelected(); setMessage('Товар додано до замовлення.'); } catch { setMessage('Не вдалося додати товар.'); } finally { setBusy(false); } };
  const deletePart = async (part) => { if (!part?.id || !window.confirm(`Видалити ${part.brand || ''} ${part.article || ''} із замовлення?`)) return; setBusy(true); try { await api.delete(`/api/order-parts/${part.id}/`); await refreshSelected(); setMessage('Товар видалено із замовлення.'); } catch { setMessage('Не вдалося видалити товар.'); } finally { setBusy(false); } };
  const patchOrder = async (payload) => { if (!selected?.id) return; setBusy(true); try { const res = await api.patch(`/api/visits/${selected.id}/`, payload); setSelected(res.data); setOrders((prev) => prev.map((o) => o.id === res.data.id ? res.data : o)); } catch { setMessage('Не вдалося оновити замовлення.'); } finally { setBusy(false); } };
  const updateDelivery = (patch) => { if (!selected) return; patchOrder({ delivery_data: JSON.stringify({ ...parseDelivery(selected), ...patch, mode: 'store' }) }); };
  const updatePartStatus = async (part, status) => { setBusy(true); try { await api.patch(`/api/order-parts/${part.id}/`, { status }); await refreshSelected(); } catch { setMessage('Не вдалося змінити статус товару.'); } finally { setBusy(false); } };
  const applySuggestion = async () => { const suggestion = getSuggestion(selected); const delivery = parseDelivery(selected); const payload = { status: suggestion.status || nextStatus(selected) }; if (suggestion.deliveryStatus) payload.delivery_data = JSON.stringify({ ...delivery, delivery_status: suggestion.deliveryStatus, mode: 'store' }); await patchOrder(payload); };
  const cancelOrder = async ({ reason, comment }) => { await patchOrder({ status: 'CANCELLED', delivery_data: JSON.stringify({ ...parseDelivery(selected), cancel_reason: reason, cancel_comment: comment, cancelled_at: new Date().toISOString(), mode: 'store' }) }); setModal(null); setMessage('Замовлення скасовано.'); };
  const printReceipt = (order) => { const delivery = parseDelivery(order); const parts = arr(order.parts); const totals = orderTotals(order); const w = window.open('', '_blank'); if (!w) return; w.document.write(`<html><head><meta charset="utf-8"><title>Товарний чек №${order.id}</title><style>body{font-family:Arial,sans-serif;margin:32px;color:#111}h1{margin:0 0 8px}.muted{color:#666}.grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:20px 0}table{width:100%;border-collapse:collapse;margin-top:16px}th,td{border:1px solid #ccc;padding:8px;text-align:left}th{background:#f3f4f6}.right{text-align:right}.total{font-size:20px;font-weight:700;text-align:right;margin-top:18px}.footer{margin-top:40px;text-align:center;color:#777;font-size:12px}</style></head><body onload="window.print()"><h1>Товарний чек №${order.id}</h1><div class="muted">Дата: ${new Date().toLocaleString('uk-UA')}</div><div class="grid"><div><b>Покупець:</b> ${order.client || '-'}<br><b>Телефон:</b> ${order.phone || '-'}</div><div><b>Доставка:</b> ${order.delivery_type === 'nova_poshta' ? 'Нова пошта' : 'Самовивіз'}<br><b>ТТН:</b> ${delivery.ttn || '-'}<br><b>Оплата:</b> ${paymentLabel[order.payment_status] || order.payment_status || '-'}</div></div><table><thead><tr><th>Бренд</th><th>Артикул</th><th>Назва</th><th>К-сть</th><th class="right">Ціна</th><th class="right">Сума</th></tr></thead><tbody>${parts.map((p) => { const c = partCalc(p); return `<tr><td>${p.brand || ''}</td><td>${p.article || ''}</td><td>${p.name || ''}</td><td>${qty(c.quantity)}</td><td class="right">${money(c.sell)}</td><td class="right">${money(c.revenue)}</td></tr>`; }).join('') || '<tr><td colspan="6" class="right">Товари не додані</td></tr>'}</tbody></table><div class="total">Разом: ${money(totals.revenue)}</div><div class="footer">Дякуємо за покупку!</div></body></html>`); w.document.close(); };
  const copyTtn = async () => { const ttn = parseDelivery(selected).ttn; if (!ttn) return; try { await navigator.clipboard.writeText(ttn); setMessage('ТТН скопійовано.'); } catch { window.prompt('Скопіюйте ТТН:', ttn); } };
  const quickSale = async (e) => { e.preventDefault(); setBusy(true); const delivery = { source: 'Швидкий продаж', mode: 'store', quick_sale: true, delivery_status: 'received' }; const orderPayload = { client: 'Роздрібний покупець', phone: '0000000000', plate: `SALE-${Date.now()}`.slice(0, 20), vin_code: null, status: 'COMPLETED', delivery_type: 'pickup', delivery_data: JSON.stringify(delivery), payment_status: quickSaleForm.payment_status, prepayment_amount: 0, comment: quickSaleForm.comment || 'Швидкий продаж', scheduled_datetime: new Date(`${filterDate}T12:00:00`).toISOString() }; try { const orderRes = await api.post('/api/visits/', orderPayload); const partPayload = { visit: orderRes.data.id, name: quickSaleForm.name.trim(), brand: (quickSaleForm.brand || 'Без бренду').trim(), article: (quickSaleForm.article || 'manual').trim(), supplier: (quickSaleForm.supplier || 'Швидкий продаж').trim(), buy_price: num(quickSaleForm.buy_price), sell_price: num(quickSaleForm.sell_price), quantity: num(quickSaleForm.quantity) || 1, status: 'ARRIVED' }; const partRes = await api.post('/api/order-parts/', partPayload); const completedOrder = { ...orderRes.data, parts: [partRes.data] }; setOrders((prev) => [completedOrder, ...prev]); setSelected(completedOrder); setQuickSaleForm({ ...emptyQuickSale }); setModal(null); setMessage('Швидкий продаж створено. Відкриваю чек.'); setTimeout(() => printReceipt(completedOrder), 250); } catch (error) { setMessage(error.response?.data?.error || 'Не вдалося створити швидкий продаж.'); } finally { setBusy(false); } };

  return <div className="max-w-[1600px] mx-auto p-3 md:p-8 md:pl-72 min-h-screen overflow-x-hidden pb-24">
    <div className="flex flex-col xl:flex-row xl:items-end justify-between gap-4 mb-5"><div><h1 className="text-2xl md:text-3xl font-black uppercase italic text-slate-900 flex items-center gap-3"><PackageCheck className="text-blue-600"/> Дошка замовлень</h1><p className="text-slate-500 font-semibold mt-1 text-sm md:text-base">Магазин автозапчастин: замовлення за день, товари, склад, доставка та чек.</p></div><div className="flex flex-col sm:flex-row gap-2"><button onClick={() => { setQuickSaleForm({ ...emptyQuickSale }); setModal('quickSale'); }} className="bg-emerald-600 text-white px-5 py-3 rounded-xl font-black uppercase tracking-widest text-xs flex items-center justify-center gap-2 hover:bg-emerald-700 shadow-lg shadow-emerald-100"><ShoppingCart size={16}/> Швидкий продаж</button><button onClick={() => { setOrderForm({ ...emptyOrder }); setModal('order'); }} className="bg-blue-600 text-white px-5 py-3 rounded-xl font-black uppercase tracking-widest text-xs flex items-center justify-center gap-2 hover:bg-blue-700 shadow-lg shadow-blue-100"><Plus size={16}/> Нове замовлення</button></div></div>
    {message && <div className="mb-5 rounded-2xl border border-blue-100 bg-blue-50 px-4 py-3 text-sm font-bold text-blue-700 flex items-center justify-between gap-3"><span>{message}</span><button onClick={() => setMessage('')}><XCircle size={16}/></button></div>}
    <div className="bg-white border border-slate-200 rounded-3xl shadow-sm p-3 sm:p-4 mb-5 grid grid-cols-1 xl:grid-cols-[1fr_auto] gap-3"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Пошук по № замовлення, клієнту, телефону, ТТН, артикулу, бренду..." className={fieldClass.replace('px-4', 'pl-9 pr-4')} /></div><div className="flex items-center justify-between xl:justify-end gap-2 bg-slate-50 border border-slate-200 rounded-xl px-2 py-2"><button onClick={() => setFilterDate(shiftDate(filterDate, -1))} className="p-2 bg-white border border-slate-200 rounded-lg hover:bg-blue-50"><ChevronLeft size={16}/></button><button onClick={() => setFilterDate(dateISO())} className="px-3 py-2 bg-white border border-slate-200 rounded-lg font-black text-xs uppercase flex items-center gap-2 min-w-[190px] justify-center"><CalendarDays size={15} className="text-blue-600"/> {humanDate(filterDate)} • {filtered.length}</button><button onClick={() => setFilterDate(shiftDate(filterDate, 1))} className="p-2 bg-white border border-slate-200 rounded-lg hover:bg-blue-50"><ChevronRight size={16}/></button></div></div>
    {loading ? <Empty text="Завантаження замовлень..."/> : <div className="grid grid-cols-1 xl:grid-cols-4 gap-5">{groups.map((column) => <OrderColumn key={column.key} column={column} onOpen={setSelected} />)}</div>}
    {selected && <OrderDrawer order={selected} setOrder={setSelected} busy={busy} onPatch={patchOrder} onUpdateDelivery={updateDelivery} onSuggestion={applySuggestion} onCancel={() => setModal('cancel')} partForm={partForm} setPartForm={setPartForm} onAddPart={addPart} onPartStatus={updatePartStatus} onDeletePart={deletePart} onPrint={printReceipt} onCopyTtn={copyTtn}/>} 
    {modal === 'order' && <Modal title="Нове замовлення" onClose={() => setModal(null)}><OrderForm form={orderForm} setForm={setOrderForm} onSubmit={createOrder} busy={busy}/></Modal>}
    {modal === 'quickSale' && <Modal title="Швидкий продаж" onClose={() => setModal(null)}><QuickSaleForm form={quickSaleForm} setForm={setQuickSaleForm} onSubmit={quickSale} busy={busy}/></Modal>}
    {modal === 'cancel' && selected && <CancelModal onClose={() => setModal(null)} onSubmit={cancelOrder} busy={busy}/>} 
  </div>;
}

function OrderColumn({ column, onOpen }) { const Icon = column.icon; return <div className={`rounded-3xl border shadow-sm p-4 min-h-[280px] ${columnStyle[column.tone]}`}><h3 className="font-black uppercase tracking-wider text-sm flex items-center gap-2 mb-4"><span className={`w-9 h-9 rounded-2xl flex items-center justify-center ${iconStyle[column.tone]}`}><Icon size={18}/></span>{column.title}<span className="ml-auto bg-white/80 px-3 py-1 rounded-xl text-slate-700">{column.orders.length}</span></h3><div className="space-y-3">{column.orders.map((order) => <OrderCard key={order.id} order={order} onOpen={() => onOpen(order)} />)}{!column.orders.length && <Empty text="Пусто"/>}</div></div>; }
function OrderCard({ order, onOpen }) { const delivery = parseDelivery(order); const parts = arr(order.parts); const totals = orderTotals(order); const waiting = parts.filter((p) => ['WAITING', 'IN_TRANSIT'].includes(p.status || 'WAITING')).length; return <button onClick={onOpen} className="w-full text-left bg-white/90 hover:bg-white border border-white/80 hover:border-blue-200 rounded-2xl p-4 transition-all shadow-sm"><div className="flex justify-between gap-3"><div><p className="font-black text-slate-900">№{order.id} • {order.client || 'Покупець'}</p><p className="text-xs font-bold text-slate-500 mt-1">{order.phone || '-'} • {order.plate || 'Без авто'}</p></div><span className="text-[10px] font-black uppercase text-slate-400 text-right">{orderDateText(order)}</span></div><div className="mt-3 flex flex-wrap gap-2"><Badge>{parts.length} поз.</Badge><Badge>{money(totals.revenue)}</Badge>{waiting > 0 && <Badge accent>{waiting} очікує</Badge>}{delivery.ttn && <Badge>ТТН</Badge>}</div></button>; }
function OrderDrawer({ order, setOrder, busy, onPatch, onUpdateDelivery, onSuggestion, onCancel, partForm, setPartForm, onAddPart, onPartStatus, onDeletePart, onPrint, onCopyTtn }) { const delivery = parseDelivery(order); const parts = arr(order.parts); const totals = orderTotals(order); const suggestion = getSuggestion(order); return <div className="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-50 flex justify-end"><div className="bg-white w-full max-w-4xl h-full overflow-y-auto shadow-2xl p-5 md:p-7"><div className="flex items-start justify-between gap-3 mb-5"><div><h2 className="text-2xl font-black uppercase text-slate-900">Замовлення №{order.id}</h2><p className="text-sm font-bold text-slate-500">{order.client} • {order.phone}</p></div><button onClick={() => setOrder(null)} className="p-2 bg-slate-100 rounded-xl"><X size={18}/></button></div><Summary order={order} delivery={delivery} totals={totals} /><div className="bg-blue-50 border border-blue-100 rounded-3xl p-4 mb-5 flex flex-col md:flex-row md:items-center justify-between gap-3"><p className="text-sm font-bold text-blue-800">{suggestion.text}</p><button disabled={busy} onClick={onSuggestion} className="bg-blue-600 text-white rounded-xl px-4 py-3 font-black uppercase text-xs">{suggestion.label}</button></div><div className="grid grid-cols-2 md:grid-cols-5 gap-2 mb-5"><StatusButton disabled={busy} label="Готове" onClick={() => onPatch({ status: 'DONE' })}/><StatusButton disabled={busy} label="Виконано" onClick={() => onPatch({ status: 'COMPLETED' })}/><StatusButton disabled={busy} label="Чек" onClick={() => onPrint(order)}/><StatusButton disabled={busy || !delivery.ttn} label="ТТН" onClick={onCopyTtn}/><button disabled={busy} onClick={onCancel} className="bg-rose-50 hover:bg-rose-100 text-rose-700 rounded-xl p-3 font-black uppercase text-[10px] disabled:opacity-50">Скасувати</button></div><div className="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-5"><Panel title="Покупець"><Info label="Клієнт" value={order.client}/><Info label="Телефон" value={order.phone}/><Info label="Авто / VIN" value={`${order.plate || '-'} ${order.vin_code || ''}`}/><Info label="Джерело" value={delivery.source || '-'}/></Panel><DeliveryPanel order={order} delivery={delivery} onPatch={onPatch} onUpdateDelivery={onUpdateDelivery}/></div><Panel title="Товари та прибуток"><form onSubmit={onAddPart} className="grid grid-cols-1 md:grid-cols-4 gap-2 mb-4"><FormInput required placeholder="Назва" value={partForm.name} onChange={(v)=>setPartForm({...partForm,name:v})}/><FormInput placeholder="Бренд" value={partForm.brand} onChange={(v)=>setPartForm({...partForm,brand:v})}/><FormInput placeholder="Артикул" value={partForm.article} onChange={(v)=>setPartForm({...partForm,article:v})}/><FormInput placeholder="Постачальник" value={partForm.supplier} onChange={(v)=>setPartForm({...partForm,supplier:v})}/><FormInput placeholder="Закупка" value={partForm.buy_price} onChange={(v)=>setPartForm({...partForm,buy_price:v})}/><FormInput required placeholder="Продаж" value={partForm.sell_price} onChange={(v)=>setPartForm({...partForm,sell_price:v})}/><FormInput placeholder="К-сть" value={partForm.quantity} onChange={(v)=>setPartForm({...partForm,quantity:v})}/><button disabled={busy} className="bg-blue-600 text-white rounded-xl font-black uppercase text-xs min-h-[46px]">Додати</button></form><div className="space-y-2">{parts.map((p)=><PartRow key={p.id} part={p} onStatus={onPartStatus} onDelete={onDeletePart}/>)}{!parts.length && <Empty text="Товари ще не додані"/>}</div></Panel></div></div>; }
function Summary({ order, delivery, totals }) { return <div className="grid grid-cols-2 md:grid-cols-4 xl:grid-cols-7 gap-3 mb-5"><Metric label="Статус" value={order.status || '-'} /><Metric label="Оплата" value={paymentLabel[order.payment_status] || '-'} /><Metric label="Доставка" value={deliveryStatusLabel[delivery.delivery_status || 'draft']} /><Metric label="Позицій" value={totals.count} /><Metric label="Продаж" value={money(totals.revenue)} /><Metric label="Прибуток" value={money(totals.profit)} positive={totals.profit >= 0}/><Metric label="Маржа" value={`${totals.margin.toFixed(1)}%`} positive={totals.profit >= 0}/></div>; }
function Metric({ label, value, positive }) { return <div className="bg-slate-50 border border-slate-200 rounded-2xl p-3"><p className="text-[10px] font-black uppercase text-slate-400 mb-1">{label}</p><p className={`font-black text-sm ${positive === undefined ? 'text-slate-800' : positive ? 'text-emerald-600' : 'text-rose-600'}`}>{value}</p></div>; }
function DeliveryPanel({ order, delivery, onPatch, onUpdateDelivery }) { return <Panel title="Нова пошта / доставка"><Select label="Оплата" value={order.payment_status || 'unpaid'} onChange={(v) => onPatch({ payment_status: v })} options={[['unpaid','Не оплачено'],['prepaid','Передплата'],['paid','Оплачено'],['cod','Післяплата'],['debt','Борг']]}/><Select label="Доставка" value={order.delivery_type || 'pickup'} onChange={(v) => onPatch({ delivery_type: v })} options={[['pickup','Самовивіз'],['nova_poshta','Нова пошта'],['courier','Курʼєр']]}/><div className="grid grid-cols-1 md:grid-cols-2 gap-2"><Labeled label="Місто" value={delivery.city || ''} onBlur={(v)=>onUpdateDelivery({ city: v })}/><Labeled label="Відділення / адреса" value={delivery.warehouse || ''} onBlur={(v)=>onUpdateDelivery({ warehouse: v })}/><Labeled label="Отримувач" value={delivery.recipient || ''} onBlur={(v)=>onUpdateDelivery({ recipient: v })}/><Labeled label="Телефон отримувача" value={delivery.recipient_phone || ''} onBlur={(v)=>onUpdateDelivery({ recipient_phone: v })}/><Labeled label="ТТН" value={delivery.ttn || ''} onBlur={(v)=>onUpdateDelivery({ ttn: v })}/><Select label="Статус доставки" value={delivery.delivery_status || 'draft'} onChange={(v)=>onUpdateDelivery({ delivery_status: v })} options={[['draft','Не відправлено'],['sent','Відправлено'],['received','Отримано'],['returned','Повернення']]}/><Labeled label="Вартість доставки" value={delivery.delivery_cost || ''} onBlur={(v)=>onUpdateDelivery({ delivery_cost: v })}/><Labeled label="Післяплата" value={delivery.cod_amount || ''} onBlur={(v)=>onUpdateDelivery({ cod_amount: v })}/><Labeled label="Комісія післяплати" value={delivery.cod_commission || ''} onBlur={(v)=>onUpdateDelivery({ cod_commission: v })}/></div></Panel>; }
function PartRow({ part, onStatus, onDelete }) { const c = partCalc(part); return <div className="bg-slate-50 border border-slate-100 rounded-2xl p-3"><div className="flex flex-col xl:flex-row xl:items-center justify-between gap-3"><div className="flex-1"><div className="flex items-start justify-between gap-3"><div><p className="font-black text-slate-800">{part.brand} {part.article}</p><p className="text-sm font-bold text-slate-500">{part.name} • {qty(c.quantity)} шт</p></div><button onClick={() => onDelete(part)} className="text-rose-400 hover:text-rose-600 bg-white border border-rose-100 rounded-xl p-2" title="Видалити товар"><Trash2 size={16}/></button></div><div className="flex flex-wrap gap-2 mt-2"><span className="text-[10px] font-black uppercase px-2 py-1 rounded-lg bg-blue-50 text-blue-700 border border-blue-100">{partSourceLabel(part)}</span><span className="text-[10px] font-black uppercase px-2 py-1 rounded-lg bg-slate-100 text-slate-600 border border-slate-200">{stockLabel(part)}</span></div><div className="grid grid-cols-2 md:grid-cols-4 gap-2 mt-2 text-[11px] font-black"><span>Закупка: {money(c.cost)}</span><span>Продаж: {money(c.revenue)}</span><span className={c.profit >= 0 ? 'text-emerald-600' : 'text-rose-600'}>Прибуток: {money(c.profit)}</span><span className={c.profit >= 0 ? 'text-emerald-600' : 'text-rose-600'}>Маржа: {c.margin.toFixed(1)}%</span></div></div><select value={part.status || 'WAITING'} onChange={(e)=>onStatus(part,e.target.value)} className={`${fieldClass} xl:w-44`}><option value="WAITING">До замовлення</option><option value="IN_TRANSIT">В дорозі</option><option value="ARRIVED">Отримано</option><option value="UNAVAILABLE">Відмова</option></select></div></div>; }
function QuickSaleForm({ form, setForm, onSubmit, busy }) { return <form onSubmit={onSubmit} className="space-y-4"><div className="bg-emerald-50 border border-emerald-100 rounded-2xl p-4 text-sm font-bold text-emerald-800">Швидкий продаж створює завершене замовлення, додає товар і одразу відкриває чек.</div><div className="grid grid-cols-1 md:grid-cols-2 gap-4"><Input required label="Назва товару" placeholder="Фільтр, лампа, масло..." value={form.name} onChange={(v)=>setForm({...form,name:v})}/><Input label="Бренд" placeholder="MAHLE, BOSCH..." value={form.brand} onChange={(v)=>setForm({...form,brand:v})}/><Input label="Артикул" placeholder="OC90" value={form.article} onChange={(v)=>setForm({...form,article:v})}/><Input label="Постачальник / джерело" placeholder="Склад / магазин" value={form.supplier} onChange={(v)=>setForm({...form,supplier:v})}/><Input label="Закупка" placeholder="0" value={form.buy_price} onChange={(v)=>setForm({...form,buy_price:v})}/><Input required label="Продаж" placeholder="0" value={form.sell_price} onChange={(v)=>setForm({...form,sell_price:v})}/><Input label="Кількість" placeholder="1" value={form.quantity} onChange={(v)=>setForm({...form,quantity:v})}/><Select label="Оплата" value={form.payment_status} onChange={(v)=>setForm({...form,payment_status:v})} options={[['paid','Оплачено'],['unpaid','Не оплачено'],['cod','Післяплата'],['debt','Борг']]}/></div><textarea placeholder="Коментар" value={form.comment} onChange={(e)=>setForm({...form,comment:e.target.value})} className={`${fieldClass} min-h-[90px] resize-none`}/><button disabled={busy} className="w-full bg-emerald-600 text-white rounded-xl p-4 font-black uppercase text-xs">{busy ? 'Створення...' : 'Продати і відкрити чек'}</button></form>; }
function CancelModal({ onClose, onSubmit, busy }) { const [reason, setReason] = useState(cancelReasons[0]); const [comment, setComment] = useState(''); return <Modal title="Скасувати замовлення" onClose={onClose} top><div className="space-y-4"><Select label="Причина відмови" value={reason} onChange={setReason} options={cancelReasons.map((v)=>[v,v])}/><textarea value={comment} onChange={(e)=>setComment(e.target.value)} placeholder="Коментар, якщо потрібно" className={`${fieldClass} min-h-[100px] resize-none`}/><div className="grid grid-cols-1 md:grid-cols-2 gap-2"><button onClick={onClose} className="bg-slate-100 text-slate-700 rounded-xl p-4 font-black uppercase text-xs">Не скасовувати</button><button disabled={busy} onClick={() => onSubmit({ reason, comment })} className="bg-rose-600 text-white rounded-xl p-4 font-black uppercase text-xs">Скасувати</button></div></div></Modal>; }
function OrderForm({ form, setForm, onSubmit, busy }) { return <form onSubmit={onSubmit} className="space-y-4"><div className="grid grid-cols-1 md:grid-cols-2 gap-4"><Input required label="Покупець" placeholder="ПІБ клієнта" value={form.client} onChange={(v)=>setForm({...form,client:v})}/><Input required label="Телефон" placeholder="+380..." value={form.phone} onChange={(v)=>setForm({...form,phone:v})}/><Input label="Номер авто" placeholder="АА1234ВС або залишити пустим" value={form.plate} onChange={(v)=>setForm({...form,plate:v.toUpperCase()})}/><Input label="VIN" placeholder="VIN, якщо є" value={form.vin_code} onChange={(v)=>setForm({...form,vin_code:v.toUpperCase()})}/><Select label="Джерело" value={form.source} onChange={(v)=>setForm({...form,source:v})} options={['Телефон','Сайт','Telegram','Instagram','OLX','Магазин'].map(v=>[v,v])}/><Select label="Доставка" value={form.delivery_type} onChange={(v)=>setForm({...form,delivery_type:v})} options={[['pickup','Самовивіз'],['nova_poshta','Нова пошта'],['courier','Курʼєр']]}/><Input label="Місто" placeholder="Наприклад: Київ" value={form.city} onChange={(v)=>setForm({...form,city:v})}/><Input label="Відділення / адреса" placeholder="№ відділення або адреса" value={form.warehouse} onChange={(v)=>setForm({...form,warehouse:v})}/></div><textarea placeholder="Коментар до замовлення" value={form.comment} onChange={(e)=>setForm({...form,comment:e.target.value})} className={`${fieldClass} min-h-[100px] resize-none`}/><button disabled={busy} className="w-full bg-blue-600 text-white rounded-xl p-4 font-black uppercase text-xs">{busy ? 'Збереження...' : 'Створити замовлення'}</button></form>; }
function Modal({ title, onClose, children, top }) { return <div className={`fixed inset-0 bg-slate-900/60 backdrop-blur-sm ${top ? 'z-[90]' : 'z-[70]'} overflow-y-auto p-4`}><div className="bg-white rounded-3xl w-full max-w-2xl mx-auto my-8 p-5 md:p-6 relative shadow-2xl"><button onClick={onClose} className="absolute top-4 right-4 p-2 bg-slate-100 rounded-xl"><X size={18}/></button><h2 className="text-2xl font-black uppercase mb-5 pr-10">{title}</h2>{children}</div></div>; }
function Panel({ title, children }) { return <div className="bg-white border border-slate-200 rounded-3xl p-4 shadow-sm"><h3 className="font-black uppercase text-slate-800 mb-3 text-sm">{title}</h3>{children}</div>; }
function Empty({ text }) { return <div className="text-center text-slate-400 text-xs font-black uppercase py-8 bg-white/60 rounded-2xl">{text}</div>; }
function Badge({ children, accent }) { return <span className={`text-[10px] font-black uppercase px-2 py-1 rounded-lg ${accent ? 'bg-amber-100 text-amber-700' : 'bg-white text-slate-600 border border-slate-200'}`}>{children}</span>; }
function StatusButton({ label, ...props }) { return <button {...props} className="bg-slate-100 hover:bg-blue-50 text-slate-700 hover:text-blue-700 rounded-xl p-3 font-black uppercase text-[10px] disabled:opacity-50">{label}</button>; }
function Info({ label, value }) { return <p className="text-sm mb-2"><span className="font-black text-slate-400 uppercase text-[10px] block">{label}</span><span className="font-bold text-slate-800">{value || '-'}</span></p>; }
function FormInput({ value, onChange, placeholder, required }) { return <input required={required} placeholder={placeholder} value={value} onChange={(e)=>onChange(e.target.value)} className={fieldClass}/>; }
function Input({ label, value, onChange, required, placeholder }) { return <label className="block"><span className="text-[10px] font-black uppercase text-slate-500 mb-1.5 block">{label}</span><input required={required} placeholder={placeholder} value={value} onChange={(e)=>onChange(e.target.value)} className={fieldClass}/></label>; }
function Labeled({ label, value, onBlur }) { const [v,setV]=useState(value); useEffect(()=>setV(value),[value]); return <label className="block mb-2"><span className="text-[10px] font-black uppercase text-slate-500 mb-1.5 block">{label}</span><input value={v} onChange={(e)=>setV(e.target.value)} onBlur={()=>onBlur(v)} className={fieldClass}/></label>; }
function Select({ label, value, onChange, options }) { return <label className="block mb-2"><span className="text-[10px] font-black uppercase text-slate-500 mb-1.5 block">{label}</span><select value={value} onChange={(e)=>onChange(e.target.value)} className={fieldClass}>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>; }
